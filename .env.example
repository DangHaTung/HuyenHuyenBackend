# Server Configuration
PORT=3000

# MongoDB Atlas Configuration
# Replace <username>, <password>, and cluster URL with your actual values
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/huyenhuyen?retryWrites=true&w=majority

# JWT Secret Key
# Generate a strong random key for production
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random

# Example MongoDB Atlas URI format:
# mongodb+srv://huyenhuyen:mypassword123@cluster0.abc123.mongodb.net/huyenhuyen?retryWrites=true&w=majority

# Railway Deployment Notes:
# 1. Set these environment variables in Railway dashboard
# 2. PORT will be automatically set by Railway
# 3. Make sure MONGO_URI and JWT_SECRET are set correctly